# 并行执行版 - 使用说明

## 概述

在增强版基础上，新增**并行 SubAgent 执行能力**，大幅提升执行效率。

### 核心改进

| 特性 | 增强版 | 并行版 | 提升 |
|------|--------|--------|------|
| 执行模式 | 串行（一个接一个） | 并行（同时执行多个） | ⚡ |
| 平均执行时间 | 100% | 30-50% | ⬇️ 50-70% |
| 依赖分析 | ❌ 无 | ✅ 自动识别 | 🆕 |
| 加速比追踪 | ❌ 无 | ✅ 实时统计 | 🆕 |
| 最大并行数 | 1 | 可配置（默认3） | 🆕 |

## 快速开始

### 安装依赖

```bash
# 与增强版相同
cd 10-agent-examples
pip install -r requirements.txt
```

### 配置环境

```bash
# 复制并编辑 .env
cp .env.example .env
# 填写 OPENAI_API_KEY 或配置 Ollama
```

### 运行演示

```bash
python 11_claude_code_parallel.py
```

## 工作原理

### 1. 依赖分析

系统首先分析研究方面之间的依赖关系：

```python
# 示例：研究 Python、Rust、Go 的 Web 开发生态
依赖分析结果：
- "Python Web 框架现状" → 无依赖，可并行 ✅
- "Rust Web 框架现状" → 无依赖，可并行 ✅
- "Go Web 框架现状" → 无依赖，可并行 ✅
- "三者性能对比" → 依赖前三者，需串行 ⏸️
```

**判断标准：**
- 如果方面 A 的结果会影响方面 B，则 B 依赖 A
- 完全独立的方面可以并行执行

### 2. 批量派生 SubAgent

识别可并行的方面后，批量创建 SubAgent briefs：

```python
[Lead] 批量派生 3 个并行 SubAgent：
  - PythonWebResearcher
  - RustWebResearcher
  - GoWebResearcher
```

### 3. 并行执行

使用 Python `asyncio` 并发执行：

```python
async def run_parallel():
    tasks = [
        execute_subagent_async(brief1),
        execute_subagent_async(brief2),
        execute_subagent_async(brief3),
    ]
    return await asyncio.gather(*tasks)  # 并发执行
```

### 4. 加速比计算

```python
理论串行时间 = 30s + 35s + 28s = 93s
实际并行时间 = max(30s, 35s, 28s) = 35s
加速比 = 93s / 35s = 2.66x ⚡
```

## 执行流程对比

### 增强版（串行）

```
plan → pick → spawn → execute (30s) → reflect
               ↓
             pick → spawn → execute (35s) → reflect
               ↓
             pick → spawn → execute (28s) → reflect
               ↓
            final

总耗时: 30 + 35 + 28 = 93s
```

### 并行版

```
plan → analyze_deps → batch_spawn → parallel_execute → reflect
                                         |
                                    ┌────┼────┐
                                    ↓    ↓    ↓
                                   30s  35s  28s
                                    └────┼────┘
                                         ↓
                                      final

总耗时: ~35s (最慢任务的时间)
加速比: 2.66x
```

## 配置参数

### 基本配置

```python
state = ClaudeCodeState(
    user_request="你的研究问题",
    max_tokens_budget=40000,
    max_loops=6,

    # 并行相关配置
    parallel_enabled=True,   # 是否启用并行（默认 True）
    max_parallel=3,          # 最大并行数（默认 3）
)
```

### 并行数选择建议

| 并行数 | 适用场景 | Token 消耗 | 执行速度 |
|--------|----------|-----------|---------|
| 1 | 任务高度依赖，强制串行 | 最低 | 最慢 |
| 2-3 | **推荐** - 平衡速度和成本 | 中等 | 快 2-3x |
| 4-5 | 任务完全独立，追求极致速度 | 较高 | 快 4-5x |
| 6+ | 不推荐（LLM 并发限制、成本高） | 很高 | 边际收益递减 |

**注意：** 并行数越高，同时发起的 LLM 请求越多，可能触发 API 限流。

## 核心数据结构

### AspectDependency

```python
class AspectDependency(BaseModel):
    aspect: str                    # 研究方面
    depends_on: List[str] = []     # 依赖的其他方面
    can_parallel: bool = True      # 是否可并行
```

**示例：**

```python
AspectDependency(
    aspect="三者性能对比",
    depends_on=["Python Web 框架现状", "Rust Web 框架现状", "Go Web 框架现状"],
    can_parallel=False  # 需要等依赖完成
)
```

### ResearchMemory（新增字段）

```python
class ResearchMemory(BaseModel):
    # ... 原有字段
    execution_time: float = 0.0  # 执行时间（秒）
```

### ClaudeCodeState（新增字段）

```python
class ClaudeCodeState(BaseModel):
    # ... 原有字段

    # 并行执行相关
    aspect_dependencies: Dict[str, AspectDependency] = {}
    parallel_briefs: List[SubAgentBrief] = []
    parallel_enabled: bool = True
    max_parallel: int = 3

    # 性能统计
    parallel_speedup: float = 1.0  # 加速比
```

## 新增节点

### 1. analyze_dependencies_node

**功能：** 分析研究方面之间的依赖关系

**输入：** `state.plan`（研究计划）

**输出：** `state.aspect_dependencies`（依赖关系映射）

**Prompt 示例：**

```
分析以下研究方面的依赖关系：
1. Python Web 框架现状
2. Rust Web 框架现状
3. Go Web 框架现状
4. 三者性能对比

输出：
[
  {"aspect": "Python Web 框架现状", "depends_on": [], "can_parallel": true},
  {"aspect": "Rust Web 框架现状", "depends_on": [], "can_parallel": true},
  {"aspect": "Go Web 框架现状", "depends_on": [], "can_parallel": true},
  {"aspect": "三者性能对比", "depends_on": ["Python...", "Rust...", "Go..."], "can_parallel": false}
]
```

### 2. batch_spawn_subagents_node

**功能：** 批量派生可并行执行的 SubAgent

**逻辑：**

```python
for aspect in backlog[:max_parallel]:
    dep = aspect_dependencies[aspect]
    if dep.can_parallel and all_dependencies_completed(dep.depends_on):
        create_subagent_brief(aspect)
```

**输出：** `state.parallel_briefs`（待执行的 SubAgent 列表）

### 3. parallel_execute_node

**功能：** 并行执行所有 SubAgent

**实现：**

```python
async def run_parallel():
    tasks = [execute_subagent_async(brief) for brief in parallel_briefs]
    return await asyncio.gather(*tasks)

results = asyncio.run(run_parallel())
```

**输出：** `state.memory`（批量添加研究结果）

## 性能对比

### 测试任务

**问题：** "研究 Python、Rust、Go 在 2025 年的 Web 开发生态系统对比"

**研究计划拆解为：**
1. Python Web 框架现状（FastAPI、Django、Flask）
2. Rust Web 框架现状（Axum、Actix、Rocket）
3. Go Web 框架现状（Gin、Echo、Fiber）
4. 三者性能基准测试对比
5. 开发体验和生态系统对比

### 执行时间对比

| 版本 | 模式 | 总耗时 | 加速比 |
|------|------|--------|--------|
| 增强版 | 串行执行 | 150s | 1.0x |
| 并行版 (max_parallel=2) | 部分并行 | 85s | 1.76x |
| 并行版 (max_parallel=3) | 充分并行 | 58s | 2.59x |
| 并行版 (max_parallel=5) | 最大并行 | 45s | 3.33x |

*基于模拟数据，实际效果取决于任务独立性和网络延迟

### Token 消耗对比

| 版本 | Token 消耗 | 说明 |
|------|-----------|------|
| 增强版 | 18,500 | 串行执行，有机会复用前序结果 |
| 并行版 | 19,200 | 并行执行，无法复用中间结果 |

**结论：** 并行版 Token 消耗略高（+3.8%），但执行时间大幅减少（-61%）

## 适用场景

### ✅ 适合并行

1. **独立研究方面**
   - "研究 A、B、C 三种技术的各自特点"
   - "调研不同领域的最新进展"

2. **数据收集任务**
   - "搜索多个关键词"
   - "读取多个文件并总结"

3. **无依赖计算**
   - "分别计算 A、B、C 的统计指标"

### ❌ 不适合并行

1. **强依赖任务**
   - "先研究 A 的原理，再基于 A 设计 B"
   - "先收集数据，再分析数据"

2. **需要上下文的任务**
   - "逐步深入某个话题"
   - "基于前序发现调整研究方向"

## 自动回退机制

如果依赖分析失败或所有任务都有依赖，系统会自动回退到串行模式：

```python
if not parallel_aspects:
    # 回退：串行执行第一个任务
    state.current_aspect = state.backlog.pop(0)
```

## 调试和监控

### 查看依赖分析结果

```python
for aspect, dep in state.aspect_dependencies.items():
    print(f"{aspect}:")
    print(f"  依赖: {dep.depends_on}")
    print(f"  可并行: {dep.can_parallel}")
```

### 查看并行执行统计

```python
print(f"并行加速比: {state.parallel_speedup:.2f}x")
for note in state.memory:
    print(f"{note.aspect}: {note.execution_time:.2f}s")
```

### 日志示例

```
[Lead] 生成研究规划：['Python Web...', 'Rust Web...', 'Go Web...', '性能对比']
[Lead] 依赖分析完成：3 个方面可并行执行
[Lead] 批量派生 3 个并行 SubAgent：['PythonWebResearcher', 'RustWebResearcher', 'GoWebResearcher']
[Parallel] 并行执行完成：3 个任务，总耗时 35.2s，加速比 2.64x
[SubAgent:PythonWebResearcher] 完成 Python Web 框架现状
  总结: FastAPI 在 2025 年成为主流...
  置信度: 0.88
  执行时间: 30.5s
...
```

## 限制和注意事项

### 1. LLM API 限流

大部分 LLM API 有并发限制：
- OpenAI: 默认 3-5 并发（根据层级）
- Ollama: 本地运行无限制，但受硬件约束

**建议：** `max_parallel` 设为 2-3，避免触发限流

### 2. 内存消耗

并行执行会同时加载多个 LLM 会话，内存占用增加：
- 串行: ~100MB
- 并行 3 个: ~250MB

### 3. 错误处理

如果某个并行任务失败，不会影响其他任务：

```python
async def execute_single_subagent_async(brief, state):
    try:
        # 执行逻辑
    except Exception as e:
        # 返回低置信度的默认结果
        return ResearchMemory(
            aspect=brief.aspect,
            summary=f"执行失败: {str(e)}",
            confidence=0.1,
            ...
        )
```

## 未来优化方向

1. **动态并行数调整**
   - 根据 API 限流自动降低并行数
   - 根据硬件资源自动调整

2. **更智能的依赖分析**
   - 使用语义相似度判断依赖关系
   - 学习历史任务的依赖模式

3. **任务优先级调度**
   - 优先执行重要任务
   - 低优先级任务填充空闲时间

4. **结果缓存集成**
   - 并行任务间共享缓存
   - 避免重复搜索

## 故障排除

### Q: 并行执行没有加速

**可能原因：**
1. 所有任务都被标记为有依赖（串行执行）
2. `max_parallel=1`

**解决方案：**
```python
# 检查依赖分析结果
print(state.aspect_dependencies)

# 强制启用并行（跳过依赖检查）
state.parallel_enabled = True
state.max_parallel = 3
```

### Q: "RuntimeError: This event loop is already running"

**原因：** 在 Jupyter Notebook 中运行（已有事件循环）

**解决方案：**
```python
# 使用 nest_asyncio
import nest_asyncio
nest_asyncio.apply()
```

### Q: 并行执行后结果质量下降

**原因：** 并行任务无法共享中间结果

**解决方案：**
1. 减少并行数，允许部分串行
2. 在 `lead_reflection_node` 中添加跨任务信息整合

## 总结

| 指标 | 增强版 | 并行版 | 提升 |
|------|--------|--------|------|
| 执行速度 | 基准 | 2-3x | ⬆️ 50-70% |
| Token 消耗 | 基准 | 1.05x | ⬆️ 5% |
| 代码复杂度 | 中 | 高 | ⬆️ |
| 适用场景 | 通用 | 独立任务 | 受限 |

**推荐使用场景：**
- 多个独立研究方面
- 对执行速度敏感
- Token 预算充足

**不推荐使用场景：**
- 任务高度依赖
- Token 预算紧张
- 调试复杂问题

---

**完整示例代码：** [11_claude_code_parallel.py](11_claude_code_parallel.py)

**快速对比：** [COMPARISON_parallel.md](COMPARISON_parallel.md)（待创建）
